import styled, { createGlobalStyle } from "styled-components";

//Global olarak yapılacak tasarımlarda kullanışan yapıdır.
export const GlobalCSS = createGlobalStyle`
    body{
        margin: 0px;
        padding: 0px;
        background-color: #B4D2BA;
    }


`